# MiniDB 数据库引擎执行链路源码讲解

本文以 `minidb-engine/src/main/java/minidb/DatabaseEngine.java` 为主线，按**实际运行时的调用顺序**讲解 JSON 计划如何被执行。图中的节点名称与源码中的 `type`、方法名保持一致；先读图，再沿箭头阅读下面的章节即可。

> 适用范围：计划协议 `protocolVersion = 1`。SQL 的词法、语法、语义分析、计划生成和优化位于 `DBcompiler-main`；本文从 Java 引擎收到 JSON 计划开始。

---

## 1. 总览：从 JSON 到一条语句

```mermaid
flowchart TB
    A[外部调用<br/>executeProgramJson(String json)] --> B[Json.parse<br/>program Map]
    B --> C{protocolVersion == 1?}
    C -- 否 --> C0[抛出 ProtocolMismatch]
    C -- 是 --> D[遍历 program.plans<br/>按顺序执行每一条计划]
    D --> E[executePlan(Map plan)<br/>单语句入口]
    E --> F{catalogVersion<br/>是否等于当前 catalogVersion?}
    F -- 否 --> F0[抛出 CatalogVersionMismatch]
    F -- 是 --> G[取 plan.root]
    G --> H[executeNode(Map node)<br/>语句根节点入口]
    H --> I{type == Explain?}
    I -- 是 --> J[explain(node)]
    I -- 否 --> K{activeProfiler 是否存在?}
    K -- 否 --> L[executeNodeRaw(node, type)]
    K -- 是 --> M[计时 → executeNodeRaw<br/>record 节点统计]
    L --> N[ExecutionResult]
    M --> N
    J --> N
```

### 先建立两个层次概念

这里最容易混淆的是“一个 JSON”和“一条语句”：

- `executeProgramJson` 接收的是**程序级** JSON。它的 `plans` 是数组，因此一次输入可以含 `CREATE TABLE`、`INSERT`、`SELECT` 等多条计划，并按数组顺序执行。
- `executePlan` 才是**单条语句级**入口。每个计划都带有编译时的 `catalogVersion`，以防表结构已被别的语句改变后仍执行旧计划。
- `executeNode` 是**语句根节点级**入口。根节点只能是 DDL、DML、查询根、或 `Explain`；`Filter`、`SeqScan` 一类关系算子不能直接作为语句根执行。

### 运行时的返回值与中间行

引擎把最终结果区分为两种：

| 类型 | 用途 | 典型来源 |
| --- | --- | --- |
| `CommandResult(operation, affectedRows)` | 描述一条写操作的结果 | DDL、INSERT、UPDATE、DELETE |
| `QueryResult(columns, rows)` | 描述可返回给客户端的二维结果集 | Project、Aggregate、SetOperation、EXPLAIN |

关系算子内部不直接传 `QueryResult`，而是传 `List<PlanRow>`。一个 `PlanRow` 还携带：

- `values`：当前行的值；
- `layout`：每个值来自哪个 `tableId / columnId / relationId / ordinal`；
- `rowIds`：关系实例到存储层 `RowId` 的映射，供 UPDATE、DELETE 精确修改物理记录。

这就是为什么查询根输出的是简单二维表，而关系输入阶段仍保留列身份和行身份。

---

## 2. EXPLAIN 与 EXPLAIN ANALYZE 分支

```mermaid
flowchart TB
    A[executeNode: type = Explain] --> B[explain(node)]
    B --> C[校验输出：唯一 QUERY PLAN VARCHAR 列<br/>且不携带 RowId]
    C --> D[校验 input 是合法语句根]
    D --> E[validateExplainTree(input, 0)]
    E --> F{analyze?}
    F -- 否：普通 EXPLAIN --> G[appendExplainLines<br/>生成计划树文本]
    F -- 是：EXPLAIN ANALYZE --> H[创建 activeProfiler]
    H --> I[executeNode(input)<br/>真实执行子计划]
    I --> J[executeNode / readInput<br/>记录每个实际执行节点的 rows、time、loops]
    J --> K[清除 activeProfiler]
    K --> G
    G --> L[QueryResult: QUERY PLAN]
```

`explain` 的实现位于 `DatabaseEngine.java` 的 199 行附近，关键点如下。

1. **普通 EXPLAIN 是只读的。** 它只校验和展示计划树，绝不会执行 INSERT、UPDATE、CREATE TABLE 等子计划。
2. **ANALYZE 会执行。** 如果 `analyze=true`，引擎把 profiler 放进 `activeProfiler`，然后调用 `executeNode(input)`。因此它会走与普通执行完全相同的 DDL、DML、查询逻辑；例如 `EXPLAIN ANALYZE UPDATE` 确实会更新数据。
3. **先校验、后执行。** `validateExplainTree` 在执行 ANALYZE 前走完整个展示树。这样一个格式错误的计划不会在执行了半条 DML 后才报错。
4. **统计是按具体 JSON 节点对象累计的。** `Profiler` 使用 `IdentityHashMap<Map<String,Object>, ProfileMetric>`；统计值包括返回行数、包含子节点时间的耗时、调用次数。循环或关联子查询反复执行同一节点时，`loops` 会累加。
5. **EXPLAIN 本身不被 profile。** `executeNode` 一发现 `Explain` 便直接调用 `explain`；真正被采样的是 ANALYZE 所执行的 `input` 子树。

`appendExplainLines` 用前序遍历输出：先父节点，后子节点；单输入节点跟 `input`，双输入节点（`NestedLoopJoin`、`SetOperation`）依次跟 `left`、`right`。因此缩进既是展示格式，也是读执行树的方向。

---

## 3. 语句根分发：DDL、DML 与查询

```mermaid
flowchart TB
    A[executeNodeRaw(node, type)] --> B{根节点 type}

    B --> DDL[DDL]
    DDL --> D1[CreateTable]
    DDL --> D2[AlterTable]
    DDL --> D3[DropTable]
    D1 --> DS[RecordStore 物理操作<br/>更新内存 catalog<br/>catalogVersion++ / persistCatalog]
    D2 --> DS
    D3 --> DS

    B --> DML[DML]
    DML --> M1[Insert]
    DML --> M2[Update]
    DML --> M3[Delete]
    M1 --> MV[构造最终表镜像<br/>validateFinalRows]
    M2 --> MV
    MV --> MS[records.insert / records.replace]
    M3 --> MD[records.erase]

    B --> Q[查询根]
    Q --> Q1[Project]
    Q --> Q2[Aggregate]
    Q --> Q3[SetOperation]
    Q1 --> RI[readInput(input)]
    Q2 --> RI
    Q3 --> QC[queryChild(left/right)<br/>→ executeNode]
```

`executeNodeRaw` 是根节点白名单。若把 `SeqScan`、`Filter`、`NestedLoopJoin`、`GroupBy`、`Sort`、`DerivedTable` 或 `EmptyResult` 放在顶层，它会抛出 `InvalidPlan`，因为这些节点没有独立的客户端结果语义。

### 3.1 DDL：存储层与 catalog 一起推进

#### CreateTable

`createTable` 会规范化表名，校验类型、列重名、VARCHAR 长度、列级约束和表级约束，构造 `TableSchema`。成功路径是：

1. `records.createTable(schema)` 创建存储表；
2. 写入 `tablesById` 与 `tablesByName` 两份内存索引；
3. `catalogVersion++`；
4. `persistCatalog()` 将版本、下一个表 ID、完整表定义写到 `RecordStore`。

因此 compiler 端下一条计划若仍使用旧版本 catalog，`executePlan` 会阻止它执行。

#### AlterTable

`alterTable` 先计算新的 schema，再按 action 处理：

- `AddColumn`：为全部已有行追加默认值或 NULL，先验证新表的最终行像，再替换记录；
- `DropColumn`：拒绝删除最后一列或表级约束所引用的列，再重写每一行去掉该列；
- `RenameTable`、`RenameColumn`：主要修改 catalog 元数据。

最后统一更新内存 catalog、版本号、持久化 catalog。也就是说，ALTER 的“逻辑结构变化”和“必要的物理行迁移”都在引擎层完成。

#### DropTable

`dropTable` 先收集并去重全部目标表名；`IF EXISTS` 时找不到表是 no-op。对每张实际存在的表：`records.dropTable(table.id())`、再从两个 catalog Map 删除。只要真正删掉至少一张表，才递增版本并持久化 catalog。

### 3.2 DML：先读目标行，再维护约束

#### Insert

`insert` 将计划中的单行 `values` 或多行 `rows` 统一规范为行数组，做类型转换和默认值填充。它扫描现有行，把“已有行 + 待插入行”交给 `validateFinalRows`；通过后才逐行 `records.insert`。这可以防止批量 INSERT 在 UNIQUE / PRIMARY KEY 冲突时只写入前半批。

#### Update

`update` 的输入必须带 `carriesRowId=true`。它先经 `readInput` 找到待更新的 `PlanRow`，再用 `rowIdFor` 确定物理记录。

每条 `SET` 的右值都对同一个 `oldRow` 求值，因此 `SET a=b, b=a` 会正确交换值，而不是让第二个赋值读到刚更新的 `a`。引擎随后构造全表最终镜像，调用 `validateFinalRows`，完全通过后才执行 `records.replace`。

#### Delete

`delete` 同样需要输入携带 RowId，但它不需要构造最终镜像：删除行不会破坏 NOT NULL、UNIQUE、PRIMARY KEY 等“剩余行必须满足”的约束。因此它遍历输入并直接 `records.erase`。

---

## 4. 查询根与关系算子入口

```mermaid
flowchart TB
    P[Project] --> R[readInput]
    A[Aggregate] --> R
    R --> RR[readInputRaw]
    RR --> S[SeqScan → records.scan]
    RR --> E[EmptyResult]
    RR --> DT[DerivedTable]
    RR --> F[Filter]
    RR --> J[NestedLoopJoin]
    RR --> G[GroupBy]
    RR --> SO[Sort]

    DT --> QC[queryChild]
    QC --> EN[executeNode：完整查询子计划]
    F --> R
    J --> R
    G --> R
    SO --> R

    SET[SetOperation] --> LQ[queryChild(left)]
    SET --> RQ[queryChild(right)]
    LQ --> EN
    RQ --> EN
```

`readInput` 是**关系算子**的统一入口。与 `executeNode` 一样，它只在 profiler 激活时计时并记录节点输出行数；正常执行时直接调用 `readInputRaw`。

请特别注意图中两类子节点：

- `Project` 和 `Aggregate` 的输入是 `List<PlanRow>`，所以走 `readInput`；
- `DerivedTable` 和 `SetOperation` 的子节点必须产生面向客户端的 `QueryResult`，所以走 `queryChild → executeNode`。这不是重复设计，而是两个不同数据形态之间的边界。

### 4.1 SeqScan：从 RecordStore 读取并重建执行行

`scan` 调用 `records.scan(table.id())` 得到 `StoredRow(id, values)`。它根据计划中的 `columns` 做列裁剪，并为每个保留列建立 `ColumnSlot`；然后生成：

```text
PlanRow(
  rowIds = { relationId -> storedRow.id },
  layout = [列身份信息...],
  values = [被裁剪后的列值...]
)
```

即使存储层保存整行，执行层仍只物化优化器请求的列。这是 column pruning 在执行层落地的位置。

### 4.2 EmptyResult：空行集也要保留布局

`emptyResult` 返回空列表，但会先调用 `emptyLayout` 验证列和 relation 身份。这个看似多余的动作服务于外连接：当 LEFT / RIGHT / FULL JOIN 的一边为空时，引擎仍需知道该侧有哪些列，才能构造 NULL 扩展行。

### 4.3 DerivedTable：查询结果重新变成关系

`derivedTable` 执行 `input` 查询子计划，得到 `QueryResult`，校验输出列数和每行宽度与派生表 schema 一致；随后将每个输出行重新包装为 `PlanRow`，并给列赋予派生表自己的 `relationId`。这样外层查询可以把 `FROM (SELECT ...) AS d` 当成普通关系引用。

### 4.4 Filter：SQL 三值逻辑中的“只保留 TRUE”

`filter` 递归读取输入，逐行执行 `evaluate(predicate, row)`，再使用 `predicateTrue`。

- `TRUE`：保留；
- `FALSE`：丢弃；
- `NULL` / UNKNOWN：也丢弃。

这符合 SQL 的 WHERE 语义，而不是 Java 中简单的布尔真值判断。

### 4.5 NestedLoopJoin：物化左右输入，再组合行

`nestedLoopJoin` 先分别执行左、右输入；对每个左行遍历所有右行，`combine(left, right)` 合并两侧 layout、values、rowIds，再计算连接谓词。

- `INNER`：只输出匹配组合；
- `LEFT`：左行没有匹配时，与右侧 NULL 行组合；
- `RIGHT`：结束后补上未匹配右行；
- `FULL`：同时补左、右两侧未匹配行。

`nullRowFor` 和 `layoutFor` 是外连接的关键辅助函数：即便子树零行，也能从计划重建 NULL 行应有的列布局。

### 4.6 GroupBy 与 Sort

- `groupBy` 用 `LinkedHashMap<List<Object>, PlanRow>` 保留每个 key 元组的第一行；这用于没有聚合函数、但需要 SQL `GROUP BY` 去重语义的计划。Java List 的相等比较也使两个 NULL group key 归入同一组。
- `sort` 先物化输入为可修改 ArrayList，再按多个排序项依次比较。排序项既可以是列，也可以是表达式；NULL 排序规则由 `compareValues` 按 ASC / DESC 实现。

---

## 5. 查询根如何形成最终结果

### Project

`project` 读取 `PlanRow` 输入后，先从 `output` 收集客户端可见列名。每行有两种投影方式：

- 老计划或纯列投影：从 `columns` 的列引用取值；
- 表达式投影：遍历 `expressions`，逐个调用 `evaluate`。

最后可处理 `DISTINCT` 和窗口限制（LIMIT / OFFSET 对应的 `window`），并把 `List<PlanRow>` 降为 `QueryResult(columns, rows)`。

### Aggregate

`aggregate` 的执行顺序是：

1. 校验 aggregate item 的函数、参数类型、返回类型与 group key 合法性；
2. `readInput` 读取输入；
3. 按 `groupKeys` 建立 `key → sourceRows` 映射；
4. 对每个组先求 HAVING；
5. 计算输出列：group 列、聚合函数、或聚合表达式；
6. 用 aggregate 专用 ORDER BY 排序；
7. 可选 DISTINCT 和窗口；
8. 输出 `QueryResult`。

全表聚合的空输入是专门处理的：没有 group key 时先创建一个空组，因此 `COUNT(*)` 能返回 `0`；有 group key 的空输入则没有任何组。

### SetOperation

`setOperation` 的左右输入都是完整查询，因此分别调用 `queryChild`。它先校验两侧宽度一致，再实现：

- `UNION`：拼接；非 ALL 时去重；
- `INTERSECT`：取交集；ALL 时按出现次数扣减；
- `EXCEPT`：左侧减去右侧；ALL 时也按出现次数扣减。

这也解释了为什么 SetOperation 不能接入 `readInput`：它的子节点是完整查询结果，而不是仍带 RowId 与列布局的关系中间行。

---

## 6. 表达式求值与子查询递归

```mermaid
flowchart TB
    A[evaluate(expr, PlanRow)<br/>或 evaluateAggregate(expr, groupRows)] --> B{expr.kind}
    B --> L[literal / column]
    B --> U[unary]
    B --> BI[binary]
    B --> CA[case]
    B --> IN[inSubquery]
    B --> EX[existsSubquery]
    B --> SC[scalarSubquery]
    B --> AG[aggregate：仅 evaluateAggregate]

    BI --> BV[binaryValues<br/>比较、布尔、算术、除法、LIKE]
    IN --> RS[runSubquery]
    EX --> RS
    SC --> RS
    RS --> Save[保存 correlationRow<br/>合并外层当前行]
    Save --> QC[queryChild(plan)]
    QC --> EN[executeNode<br/>重新进入完整执行链]
    EN --> Restore[finally 恢复 correlationRow]
    Restore --> Check[校验列数 / 行数 / NULL 语义]
```

### 6.1 普通表达式：evaluate

`evaluate(expression, row)` 按 `kind` 分派：

- `literal`：按计划声明的 SQL 类型强制转换；
- `column`：按 `ColumnSlot` 身份从当前行取值；
- `unary`：NOT、IS NULL、IS NOT NULL、负号；
- `binary`：先求左右表达式，再按操作符计算；
- `case`：按 CASE 分支顺序求值；
- `inSubquery`、`existsSubquery`、`scalarSubquery`：进入子查询路径；
- `aggregate`：普通逐行上下文不允许，直接报计划错误。

`binary` 的 `op` 才承载具体运算：`And`、`Or`、比较、`Add`、`Subtract`、`Multiply`、`Divide`、`Like`。其中 AND / OR 会短路；除法检测零除和整数溢出；LIKE 使用 `%` 与 `_` 的模式匹配。

### 6.2 聚合表达式：evaluateAggregate

`evaluateAggregate(expression, rows)` 的树形结构与 `evaluate` 类似，但它多支持 `aggregate` 叶子，例如 `COUNT`、`SUM`、`AVG`、`MIN`、`MAX`。这些叶子消费的是整个组的 `rows`。

如果聚合表达式需要普通列，它使用该组第一条源行作为列上下文；空全局聚合的普通列输出则会被上层拒绝。这样既能计算 `SUM(a) / COUNT(*)`，也能保持 group 列引用规则。

### 6.3 子查询：回到完整执行链

`runSubquery` 是本图最重要的递归闭环：

1. 保存旧 `correlationRow`；
2. 用 `mergeCorrelation` 合并旧外层作用域与当前行；
3. `queryChild(expression.plan)`，即 `executeNode` 执行一棵完整查询计划；
4. 无论成功还是异常，都在 `finally` 中恢复旧作用域；
5. 调用方按子查询类型验证结果。

三种子查询的结果规则：

| 表达式 | 必要条件 | 返回规则 |
| --- | --- | --- |
| `IN (subquery)` | 子查询必须一列 | 匹配返回 TRUE；无匹配但有 NULL 比较返回 UNKNOWN；否则 FALSE；NOT IN 反转确定结果 |
| `EXISTS (subquery)` | 不要求列数 | 只看是否至少一行；NOT EXISTS 取反 |
| 标量子查询 | 必须一列、至多一行 | 零行返回 NULL；一行返回该值；多行抛 `ScalarSubqueryCardinality` |

关联子查询的列查找也依赖这里的 `correlationRow`：当前子查询行找不到所需的 `ColumnSlot` 时，`columnValue` 可以回退到外层合并后的作用域。

---

## 7. 一次 SELECT 的阅读示例

设计划的逻辑形态为：

```sql
SELECT d.name
FROM users AS d
WHERE d.id IN (SELECT o.user_id FROM orders AS o WHERE o.amount > 100)
ORDER BY d.name;
```

可以按以下顺序把源码串起来：

1. `executeProgramJson → executePlan` 校验协议和 catalog 版本；
2. 根 `Project → project`；
3. `Project.input` 是 `Sort → readInput`；
4. `Sort.input` 是 `Filter → readInput`；
5. `Filter.input` 是 `SeqScan → records.scan(users)`，产生带 relation identity 的 `PlanRow`；
6. Filter 对每个 user 行 `evaluate(inSubquery, userRow)`；
7. `runSubquery` 把 user 行安装为关联上下文，`queryChild → executeNode(Project)` 执行 orders 子查询；
8. orders 子查询内部又是 `Project → Filter → SeqScan` 的完整链；
9. 子查询返回一列 user_id，`inSubquery` 按 SQL NULL 语义决定当前 users 行是否保留；
10. Filter 结果回到 Sort 排序，再回到 Project 取 name；
11. Project 把最终 `PlanRow` 转成 `QueryResult` 返回客户端。

如果这条语句改为 `EXPLAIN ANALYZE`，第 1 步先进入 EXPLAIN 分支；第 2–11 步仍不变，只是所有实际经过的 `executeNode`、`readInput` 节点都会多一份 profile 统计，最终以 `QUERY PLAN` 文本返回。

---

## 8. 读图时最值得记住的边界

1. **`executeNode` 执行语句形节点；`readInput` 执行关系形节点。**
2. **`QueryResult` 是查询边界的二维结果；`PlanRow` 是关系树内部、保留身份信息的执行行。**
3. **`queryChild → executeNode` 是三处递归入口：派生表、集合运算、表达式子查询。**
4. **catalogVersion 保护“计划使用的 schema”与“当前 schema”的一致性。**
5. **EXPLAIN 不执行；EXPLAIN ANALYZE 执行且采样。**
6. **INSERT / UPDATE 先验证最终表状态，DELETE 则直接按 RowId 删除。**

按这六个边界阅读源码，调用链会始终保持清晰：从 JSON 计划进入，先决定它是语句、关系还是表达式；遇到派生查询或子查询时，经 `queryChild` 回到语句入口；最终再回到上层节点继续完成结果。

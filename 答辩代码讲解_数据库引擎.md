# MiniDB 数据库系统（数据库引擎）源码讲解（答辩版）

> 适用对象：负责**数据库系统（数据库引擎）**模块的成员。
> 讲解口径：先讲“我的模块在整条 SQL 链路里的位置和职责边界”，再逐层讲我负责的执行引擎、存储引擎与目录、B+ 树索引与 DML 维护。
> 全文引用均为项目内真实路径，可 IDE 打开核对。

---

## 0. 我负责什么（最重要，先讲清边界）

对照《大型平台软件设计实习评分-2026》，评分模块分成三个：
- **SQL 编译器（16 分）**：词法/语法/语义/执行计划 —— 由**编译器同学**负责。
- **存储系统（12 分）**：页式存储管理、缓存机制、接口与集成 —— 由**存储系统同学**负责，对应代码 `minidb-engine/.../storage/` 包（Page、BufferPool、LRU、DiskManager）。
- **数据库系统（12 分）**：执行引擎、存储引擎与目录、系统集成 —— **就是我负责的“数据库引擎”**。

### 我的一句话
> 编译器把 SQL 编译成 JSON 逻辑执行计划，我负责 **解析并执行这个计划**；执行要用真实数据时，我实现并管理 **行存储、系统目录和 B+ 树索引**；底层页面的真正落盘与缓存由存储系统的 `BufferPool` 提供，我通过统一接口调用它。

### 我的代码 vs 不属于我的代码
| 归属 | 具体类/包 |
| --- | --- |
| ✅ 我：执行引擎 | `DatabaseEngine` |
| ✅ 我：存储边界接口 | `RecordStore` |
| ✅ 我：存储引擎与目录 | `PageRecordStore`、`InMemoryRecordStore` |
| ✅ 我：索引 | `storage/index/`（BPlusTreeIndex、RowId、IntIndex、IndexEntry） |
| ⛔ 存储系统（我调用） | `storage/`（BufferPool、BufferPoolManager、PageGuard、Lru/Fifo、DiskManager） |
| ⛔ 编译器（队友） | `DBcompiler-main/`（C++） |

> **答辩关键表述**：页缓存和磁盘文件是存储系统的职责；但我负责的 `PageRecordStore` 和 `BPlusTreeIndex` 都**依赖 BufferPool 提供的 4KB 页接口**（`newPageGuard/getPageGuard`）来读写页。这是我与存储系统之间的交接点。

---

## 1. 我的模块在整体架构中的位置

```text
C++ 编译器 (队友) ──► JSON 逻辑执行计划
                            │
                            ▼
        ┌───────────────────────────────────────────┐
        │             我（数据库系统）               │
        │  DatabaseEngine 执行引擎                  │
        │     │ 用 RecordStore 接口读写             │
        │     ▼                                    │
        │  PageRecordStore 存储引擎与目录           │
        │  BPlusTreeIndex 索引 / DML 维护          │
        │     │ 通过 BufferPool 接口调用页          │
        └─────┼─────────────────────────────────────┘
              ▼
        存储系统（队友）：BufferPool + DiskManager（落盘文件）
```

---

## 2. 我的第一层：存储边界接口 `RecordStore`

执行层不知道数据放内存还是磁盘，它只依赖 `RecordStore` 的 5 个能力（[RecordStore.java](minidb-engine/src/main/java/minidb/RecordStore.java)）：

```java
createTable / dropTable / insert / scan / replace / erase
```
- 内存实现 `InMemoryRecordStore`：测试用。
- 磁盘实现 `PageRecordStore`：生产默认。

> **答辩可讲**：这是一个「存储实现可替换」的边界。只要实现这个接口，执行层完全不用改。

---

## 3. 我的第二层：执行引擎 `DatabaseEngine`

### 3.1 入口与分派
`executeProgramJson → executePlan → executeNode`（[DatabaseEngine.java#L152-L196](minidb-engine/src/main/java/minidb/DatabaseEngine.java#L152-L196)）：
- `executePlan` 先比较 **catalogVersion**，防止用旧目录编译的计划执行（返回 `CatalogVersionMismatch`）。
- `executeNodeRaw` 是一个 `switch` 分派：`CreateTable/AlterTable/DropTable/Insert/Project/Aggregate/SetOperation/Update/Delete`。
- 查询算子继续下钻 `readInput`：`SeqScan / Filter / NestedLoopJoin / GroupBy / Sort`。
- `EXPLAIN ANALYZE` 用 `Profiler` 按**计划节点对象**累计行数/耗时/循环次数，再打印成树。

> **答辩可讲**：这是典型的解释器结构（switch 把计划类型映射到执行函数），加算子只需加 case。

### 3.2 目录与约束（写入原子性）
- 运行时持有 `tablesById / tablesByName` 两份目录，记录每张表的 `TableSchema`（列 + 约束）。
- `insert()`：先 scan 出“现有行 + 新行”拼成**最终假想表** → `validateFinalRows` 全量校验 NOT NULL / 主键 / 唯一 → 通过后才真正落盘。
- `update()`：同样先 `scan` 出替换后的整表校验，再写回。
- `DELETE`：按携带的 RowId 逐条 `erase`。

> **答辩可讲**：约束“先校验、后写入”，保证失败不会留下半条脏数据（原子性）。

### 3.3 执行与行模型
- 内部行模型 `PlanRow = (rowIds, 列布局 layout, 值 values)`；列用 `ColumnSlot(tableId,columnId,relationId,ordinal,type)` 区别不同表的同名列（Join 里很重要）。
- 存储层保存**完整行**，执行层只物化优化器裁剪的列（`SeqScan.columns` 契约，见 `scanLayout`）。

---

## 4. 我的第三层：存储引擎与目录 `PageRecordStore`

> 这一层对应评分表「数据库系统」里的**存储引擎与目录**（记录和页面的存取、序列化反序列化、数据持久化、系统目录）。页本身的落盘交给存储系统的 BufferPool。

### 4.1 三类页 + magic
每页固定 **4KB=4096B**。用 magic 区分：
| 页 | magic | 用途 |
| --- | --- | --- |
| 超块 superblock | `MDBS` | 第 0 页，存 catalog 根页号 |
| catalog 目录 | `MDCC` | 表结构（链式多页，页头带 next） |
| data 数据页 | `MDDP` | 槽位页，存真实记录 |

### 4.2 data 页：槽位页布局
```text
偏移 0        4          8             12
[ magic ][ reserved ][ 数据区末端 end ][ 槽数 ]
每 12 字节一个槽位：[ 数据偏移 ][ 长度 ][ 1=有效 ]
```
- 数据从**页尾 end 从后往前压**；槽位表从**页头 12 从前往后长**。
- `append` 判断“槽位表 + 新数据是否重叠”，不重叠才写。
- 一条记录 **RowId = (pageId, slotId)**，用 `pack/unpack` 编成 `long`（高32位页号，低32位槽号）。
- `erase` 只需把槽位有效标志置 0，空出的空间后续可复用。

> **答辩必答**：槽位页解决“数据偏移反复移动”问题——数据从尾压、槽位从头长，中间留出的空洞可复用。

### 4.3 行编码 encodeRow / decodeRow
紧凑二进制：列数 → 每列 type tag：
```
0=NULL  1=INT(8B)  2=FLOAT(8B)  3=VARCHAR(len+UTF8bytes)  4=BOOL(1B)
```
单行长度的校验绑定 `PAGE_SIZE`——保证单行能放进一个数据页。

### 4.4 系统目录的持久化
- `persistCatalog`：把表结构序列化成 JSON，写进一连串 catalog 页（next 指针串链），起始页号写回 superblock（第 0 页）。
- 启动 `loadCatalog`：superblock → catalog 根页 → 顺链读回 → 解析 JSON → 恢复全部表结构。
- 这就是“正常关闭后重启数据还在”的机制：`initialize()` 保证第 0 页是 superblock。

---

## 5. 我的索引：B+ 树 `BPlusTreeIndex`（独立能力）

`minidb.storage.index.BPlusTreeIndex`：持久化唯一 B+ 树，key=int → RowId。

### 5.1 结构
- 元数据页 `BPTM` 稳定保存 `rootPageId + size`（root 因分裂变化只改这一页）。
- 叶子页 `BPTN`：`keys[] + rowIds[]`，用 `nextLeafPageId` 串成**叶子链** → 支撑范围扫描。
- 内部页：`children[] + keys[]`，`children = keys + 1`。

### 5.2 关键算法
- 查：root 起按 `upperBound` 下钻到叶子，`lowerBound` 定位；`range` 顺叶子链右扫。
- 插：递归下钻，叶子满则 `splitLeaf`（拿右半 + 分隔键上抛），内节点满则 `splitInternal`；**root 分裂才长高**，只改元数据页。
- 删：目前只做叶子删除，**未做下溢合并/兄弟借位/根收缩**（简化实现）。

> **答辩诚实口径**：这个 B+ 树算法和持久化已完成、有自测，但**目前尚未接入 SQL**——即还没有 `CREATE INDEX`、没有 `IndexScan`。它是我接下来 P1–P5 的工作。README 也明确说了“不要声称 SQL 已用索引”。

---

## 6. 我与编译器同学的接口（我交付的契约）

对应接口规范 [MiniDB 索引编译执行接口规范.docx](MiniDB 索引编译执行接口规范.docx)：

- **Catalog 快照导出**：`catalogSnapshotJson()` / `catalogSnapshotFileText()` 输出表、列的**稳定 ID**；新增索引能力时输出 `IndexSchema`（索引名、表列 ID、keyType、unique、metadataPageId）的 `I` 记录，供编译器读取。
- **计划执行约定**：`CreateIndex / DropIndex / IndexScan` 三类 JSON 节点的字段、`protocolVersion=1`、catalogVersion 递增规则，由我们双方共同确认，Java 端执行。
- **能力边界声明**：第一版只支持 INT 单列唯一索引，避免编译器生成 Java 尚不支持的计划。

---

## 7. 我的待办：把 B+ 树接入 SQL（P1–P5，也是创新拿分点）

这是我模块最实在的下一步，也是创新点：
| 优先级 | 任务 | 完成定义 |
| --- | --- | --- |
| P1 | 定义 `IndexSchema` + 索引目录持久化；RowId 读取 + `replace` 返回最终 RowId | 索引元数据可写、可读、可恢复 |
| P2 | Java 实现 `CreateIndex/DropIndex` + 建索引时扫描已有行**回填 B+ 树** | 已有表能建 INT 单列唯一索引 |
| P3 | INSERT/UPDATE/DELETE/DROP TABLE **自动维护索引**；重复键报错且不留半写入 | DML 后索引与顺序扫描一致 |
| P4 | 执行 `IndexScan`（读 RowId 取回记录，等值+范围） | EXPLAIN ANALYZE 出现 IndexScan |
| P5 | B+ 树删除下溢、兄弟借位、节点合并、根收缩、空页回收、destroy、页损坏校验 | 随机插删/重启/长删后仍正确 |

验收脚本：`CREATE INDEX idx_demo_id ON demo_idx(id)` → `EXPLAIN ANALYZE SELECT * FROM demo_idx WHERE id=1` 应出现 `IndexScan[idx_demo_id]`，且与 SeqScan 结果一致。

---

## 8. 答辩常见 Q&A（按我模块口径）

**Q：你的数据重启为什么还在？**
A：我通过 BufferPool 改页并标脏；更关键的是我的 **系统目录**在 superblock 里持久化了 catalog 根页，启动时顺链读回全部表结构。真正的页落盘 flush 由存储系统在关闭时统一完成。

**Q：更新长 VARCHAR 后 RowId 会变吗？**
A：目前 `PageRecordStore.replace` 在原槽放不下时会把行**搬迁**到新页（旧槽置 0 再 insert）。所以 RowId 不保证稳定——这正是我 P1 要解决的：让 replace 返回最终 RowId，且索引映射要同步。

**Q：一次 UPDATE 可能部分成功吗？**
A：不会。先 scan 出替换后的整表并全量校验，通过才写回；约束失败直接抛错不落盘。

**Q：你的 B+ 树用了吗？**
A：算法已完成、有自测，但还没被 SQL 调用（没有 CREATE INDEX/IndexScan）。这是我校验里要补的 P1–P5。目前默认走 SeqScan 全表扫描。

**Q：为什么用 RecordStore 接口？**
A：执行层与存储实现解耦。换内存/磁盘实现，执行层不改。

**Q：你和存储系统同学怎么分界？**
A：我负责行、目录、索引和 DML；页面真正的缓冲与落盘文件由存储系统的 BufferPool/DiskManager 负责，我通过 `newPageGuard/getPageGuard` 调用它。

**Q：单行太大怎么办？**
A：所有列必须能塞进一个 4KB 页（编码校验绑 PAGE_SIZE），放不下抛 “row is too large”。

---

## 9. 我的答辩主线（30 秒话术）

> 我负责数据库系统：编译器给我 JSON 计划，我用 DatabaseEngine 解释执行；真实数据由 PageRecordStore 用 4KB 槽位页落成行、用 superblock + catalog 目录持久化；我有独立的 B+ 树索引作为索引能力。核心难点是**约束先校验后写入的原子性**、**槽位页的偏移管理**、**RowId 稳定与索引同步**，以及**B+ 树分裂上抛与元数据持久化**。下一步我把 B+ 树接入 SQL（CreateIndex/IndexScan/DML 维护），这是我的创新亮点。

---

## 附：诚实边界（答辩别说错）
- 无 WAL、无崩溃自动恢复、无并发事务（只保证正常关闭后持久化）。
- 存储系统（页缓存、磁盘、LRU/FIFO）不是我负责，我消费 BufferPool。
- SQL 编译器（词法/语法/语义/物理计划、CTest 段错误）不是我负责，是编译器同学的活。
- B+ 树目前独立存在，尚未被 SQL 调用；不要声称“SQL 已使用索引”。